@using Microsoft.AspNetCore.Identity
@using WebApplication1.Areas.Identity.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav nav-underline">
    @if (SignInManager.IsSignedIn(User))
    {
        <div style="display: inline-flex; margin-top:0.2rem;">
            <div class="form-inline">
                @if (User.IsInRole("Admin"))
                {
                    <a id="manage" class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                        <i class="fa-solid fa-user-tie" style="color: #1e8a42;margin-inline:0.3rem;"></i>
                    </a>
                }
                else
                {
                    <a id="manage" class="nav-link" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
                        <i class="fa-solid fa-user" style="color: #1e8a42;margin-inline:0.3rem;"></i>
                    </a>
                }
            </div>
            <div class="form-check-inline">
                <a href="#" class="nav-link">
                    <i style="color: #1e8a42;margin-inline:0.3rem;" class="fa-solid fa-cart-shopping"><span style="position:absolute;margin-inline:0.1rem;" class="badge rounded-pill text-bg-warning">0</span></i>
                </a>
            </div>
            <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="nav-link"><i style="color: #1e8a42;margin-right:0.2rem;margin-left: 0.5rem;" class="fa-solid fa-right-from-bracket"></i></button>
            </form>
            <div class="nav-item search-logo">
                <a class="nav-link" asp-controller="Home" asp-action="Products">
                    <i style="color: #1e8a42;margin-inline:0.3rem;" class="fa-solid fa-magnifying-glass"></i>
                </a>
            </div>
        </div>
    }
    else
    {
        <li style="margin-top:0.2rem;" class="nav-item">
            <a class="nav-link small-nav" id="login" asp-area="Identity" asp-page="/Account/Login"><i style="color: #1e8a42;margin-inline:0.6rem;" class="fa-solid fa-right-to-bracket"></i></a>
        </li>
    }
</ul>
